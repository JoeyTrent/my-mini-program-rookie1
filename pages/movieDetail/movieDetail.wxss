/* pages/movieDetail/movieDetail.wxss */
.movieDetailContainer {
   display: flex;
   flex-direction: column;
   align-items: center;
}

.movieImg {
  width: 70%;
  height: 700rpx;
  margin: 20rpx 0;
}

.movieName {
  font-size: 40rpx;
  flex-wrap: blod;
}

.movie_detail {
  display: flex;
  flex-direction: column;
  font-size: 32rpx;
}

button {
  width: 70%;
  height: 80%;
  background: green;
  color: #fff;
  line-height: 80rpx;
  margin-top: 50rpx;
}